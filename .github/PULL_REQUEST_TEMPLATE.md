## What this changes

(One paragraph.)

## Why

(The user-visible problem this fixes / improves.)

## Testing

- [ ] `uv run pytest tests/` passes
- [ ] If backend: added/updated test in `tests/`
- [ ] If frontend visual: manually verified in browser; describe what to look for

## Checklist

- [ ] No secrets committed
- [ ] Docs updated if behavior visible to users
- [ ] If adds runtime dep: install scripts updated
